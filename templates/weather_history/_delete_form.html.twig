<form method="post" action="{{ path('app_weather_history_delete', {'id': weather_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weather_history.id) }}">
    <button class="btn">Delete</button>
</form>
