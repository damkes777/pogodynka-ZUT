{% extends 'base.html.twig' %}

{% block title %}WeatherHistory{% endblock %}

{% block body %}
    <h1>WeatherHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weather_history.id }}</td>
            </tr>
            <tr>
                <th>Temperature</th>
                <td>{{ weather_history.temperature }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ weather_history.unit }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ weather_history.createdAt ? weather_history.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_weather_history_index') }}">back to list</a>

    <a href="{{ path('app_weather_history_edit', {'id': weather_history.id}) }}">edit</a>

    {{ include('weather_history/_delete_form.html.twig') }}
{% endblock %}
